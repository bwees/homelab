apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-library
  namespace: media
spec:
  accessModes: [ReadWriteMany]
  storageClassName: ''
  volumeName: nas-media-library
  resources:
    requests:
      storage: 1Ti
