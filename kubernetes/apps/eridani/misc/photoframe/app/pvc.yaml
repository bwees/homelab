apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoframe-data
  namespace: misc
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: miroir
  resources:
    requests:
      storage: 2Gi
