apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoframe-data
  namespace: misc
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
