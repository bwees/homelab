---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: immich
  values:
    valkey:
      enabled: true
      controllers:
        main:
          containers:
            main:
              resources:
                requests:
                  cpu: 10m
                  memory: 32Mi
                limits:
                  memory: 256Mi

    immich:
      persistence:
        library:
          existingClaim: immich-library

    defaultPodOptions:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        supplementalGroups:
          - 44
          - 303

    controllers:
      main:
        containers:
          main:
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - NET_RAW
            env:
              IMMICH_MACHINE_LEARNING_URL: http://immich-ml.tail72746.ts.net:3003
              DB_HOSTNAME:
                valueFrom:
                  secretKeyRef:
                    name: immich-db-superuser
                    key: host
              DB_USERNAME:
                valueFrom:
                  secretKeyRef:
                    name: immich-db-superuser
                    key: username
              DB_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: immich-db-superuser
                    key: password
              TZ: America/New_York
            image:
              # renovate: datasource=docker depName=ghcr.io/immich-app/immich-server
              tag: 'v3.2.2'

    server:
      controllers:
        main:
          containers:
            main:
              resources:
                requests:
                  cpu: 100m
                  memory: 512Mi
                limits:
                  memory: 3Gi
                  gpu.intel.com/i915: '1'
      persistence:
        nas-media:
          existingClaim: nas-media
          globalMounts:
            - path: /nas-media
              readOnly: true
      route:
        main:
          parentRefs:
            - name: envoy-ingress
              namespace: networking
          hostnames:
            - immich.home.bwees.dev
            - album.bwees.io
          rules:
            - backendRefs:
                - identifier: main
                  port: 2283

    machine-learning:
      enabled: false
