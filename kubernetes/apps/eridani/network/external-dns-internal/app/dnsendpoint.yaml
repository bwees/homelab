---
# Apex NS glue for wees.home; all other records come dynamically from HTTPRoutes.
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: static-records
spec:
  endpoints:
    - dnsName: ns1.wees.home
      recordType: A
      targets:
        - 10.0.1.2
