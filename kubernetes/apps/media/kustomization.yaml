apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Media-wide resources. Individual apps (jellyfin, storage, …) are wired per
# cluster under clusters/<cluster>/apps/.
resources:
- namespace.yaml
