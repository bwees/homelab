endpoints:
  - name: homelab-vps
    group: vps
    url: 'icmp://100.105.77.106'
    interval: 1m
    conditions:
      - '[CONNECTED] == true'
    alerts:
      - type: discord

  - name: beszel
    group: vps
    url: 'https://beszel.bwees.dev/'
    interval: 1m
    conditions:
      - '[STATUS] == 200'
    alerts:
      - type: discord

  - name: dns
    group: vps
    # confirms external-dns is still publishing into the bwees.dev Cloudflare zone
    url: '1.1.1.1'
    dns:
      query-name: 'nas.bwees.dev'
      query-type: 'A'
    conditions:
      - '[BODY] == 192.168.50.4'
      - '[DNS_RCODE] == NOERROR'
    alerts:
      - type: discord
