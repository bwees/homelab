---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: powerdns
spec:
  chartRef:
    kind: OCIRepository
    name: powerdns
  interval: 1h
  values:
    controllers:
      main:
        initContainers:
          # Create the bwees.lab zone if absent; also hold wees.home, whose
          # records are pushed here by the eridani cluster's external-dns.
          zone-init:
            image:
              repository: powerdns/pdns-auth-49
              tag: '4.9.16'
            command:
              - sh
              - -c
              - >-
                pdnsutil list-zone bwees.lab >/dev/null 2>&1 ||
                pdnsutil create-zone bwees.lab ns1.bwees.lab;
                pdnsutil list-zone wees.home >/dev/null 2>&1 ||
                pdnsutil create-zone wees.home ns1.wees.home
        containers:
          main:
            image:
              repository: powerdns/pdns-auth-49
              tag: '4.9.16'
            env:
              PDNS_AUTH_API_KEY:
                valueFrom:
                  secretKeyRef:
                    name: powerdns-api
                    key: PDNS_API_KEY
            resources:
              requests:
                cpu: 10m
                memory: 64Mi
              limits:
                memory: 128Mi

    service:
      main:
        controller: main
        # operator exposes this as a dedicated tailnet device (L3 TCP/UDP DNAT)
        annotations:
          tailscale.com/expose: 'true'
          tailscale.com/hostname: dns
        ports:
          dns-tcp:
            port: 53
            protocol: TCP
          dns-udp:
            port: 53
            protocol: UDP
          api:
            port: 8081
            protocol: TCP

    defaultPodOptions:
      securityContext:
        runAsUser: 953
        runAsGroup: 953
        fsGroup: 953
        fsGroupChangePolicy: OnRootMismatch

    persistence:
      data:
        existingClaim: powerdns-data
        advancedMounts:
          main:
            zone-init:
              - path: /var/lib/powerdns
            main:
              - path: /var/lib/powerdns

      config:
        type: configMap
        name: powerdns-config
        advancedMounts:
          main:
            zone-init:
              - path: /etc/powerdns/pdns.conf
                subPath: pdns.conf
            main:
              - path: /etc/powerdns/pdns.conf
                subPath: pdns.conf
