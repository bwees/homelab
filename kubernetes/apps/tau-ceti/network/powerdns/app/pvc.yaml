apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: powerdns-data
  namespace: network
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 256Mi
