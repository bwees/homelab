apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: questarr-config
  namespace: media
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: miroir
  resources:
    requests:
      storage: 500Mi
