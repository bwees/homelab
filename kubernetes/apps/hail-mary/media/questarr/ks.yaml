apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: questarr
spec:
  interval: 1h
  path: ./kubernetes/apps/hail-mary/media/questarr/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  dependsOn:
    - name: miroir-config
      namespace: storage
    - name: volsync
      namespace: storage
    - name: onepassword
      namespace: secrets
    - name: media-library
    - name: tailscale
  components:
    - ../../../../../components/volsync
  postBuild:
    substitute:
      APP: questarr
      VOLSYNC_CLAIM: questarr-config
  targetNamespace: media
