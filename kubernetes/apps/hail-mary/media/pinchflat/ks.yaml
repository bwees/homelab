apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: pinchflat
spec:
  interval: 1h
  path: ./kubernetes/apps/hail-mary/media/pinchflat/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  dependsOn:
    - name: miroir-config
      namespace: storage
    - name: volsync
      namespace: storage
    - name: onepassword
      namespace: secrets
    - name: media-library
  components:
    - ../../../../../components/volsync
  postBuild:
    substitute:
      APP: pinchflat
      VOLSYNC_CLAIM: pinchflat-config
  targetNamespace: media
