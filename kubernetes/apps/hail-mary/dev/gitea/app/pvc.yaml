apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-data
  namespace: dev
spec:
  accessModes: [ReadWriteMany]
  storageClassName: ''
  volumeName: nas-gitea
  resources:
    requests:
      storage: 100Gi
