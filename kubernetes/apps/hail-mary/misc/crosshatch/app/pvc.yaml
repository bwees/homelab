apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crosshatch-data
  namespace: misc
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
