apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: futomarket-data
  namespace: misc
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: miroir
  resources:
    requests:
      storage: 100Mi
