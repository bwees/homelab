apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slopvm-data
  namespace: misc
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: miroir
  resources:
    requests:
      storage: 20Gi
