apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mosquitto
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: mosquitto
  values:
    controllers:
      main:
        strategy: Recreate
        containers:
          main:
            image:
              repository: docker.io/library/eclipse-mosquitto
              tag: '2.1.2-alpine'
            resources:
              requests:
                cpu: 10m
                memory: 32Mi
              limits:
                memory: 128Mi
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: 1883
                  timeoutSeconds: 5
                  periodSeconds: 10
                  failureThreshold: 3
              readiness: *probes

    defaultPodOptions:
      # IoT devices dial 1883 by IP, so the broker takes the node's own port
      # rather than a NodePort. That pins it to one node, hence Recreate.
      hostNetwork: true
      nodeSelector:
        kubernetes.io/hostname: xenonite
      securityContext:
        runAsUser: 1883
        runAsGroup: 1883
        runAsNonRoot: true
        fsGroup: 1883
        fsGroupChangePolicy: OnRootMismatch

    service:
      main:
        controller: main
        # IoT devices reach the broker over xenonite's iot0 VLAN interface, which
        # is not one of the node's Kubernetes addresses, so the record is written
        # by hand rather than derived from the Service.
        annotations:
          external-dns.alpha.kubernetes.io/hostname: mqtt.bwees.dev
          external-dns.alpha.kubernetes.io/target: 192.168.30.106
        ports:
          mqtt:
            port: 1883

    persistence:
      data:
        existingClaim: mosquitto-data
        globalMounts:
          - path: /mosquitto/data
      config:
        type: configMap
        name: mosquitto-config
        globalMounts:
          - path: /mosquitto/config/mosquitto.conf
            subPath: mosquitto.conf
