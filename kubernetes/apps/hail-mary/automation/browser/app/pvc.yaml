apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: browser-config
  namespace: automation
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: miroir
  resources:
    requests:
      storage: 10Gi
