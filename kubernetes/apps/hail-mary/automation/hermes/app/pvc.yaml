apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hermes-data
  namespace: automation
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: miroir
  resources:
    requests:
      storage: 10Gi
