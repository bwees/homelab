apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ocirepository.yaml
  - helmrelease.yaml

configMapGenerator:
  - name: wolf-config
    files:
      - config.toml

generatorOptions:
  disableNameSuffixHash: true
