apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wakapi-data
  namespace: misc
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Mi
