
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-config
  namespace: media
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-hostpath 
  resources:
    requests:
      storage: 20Gi
