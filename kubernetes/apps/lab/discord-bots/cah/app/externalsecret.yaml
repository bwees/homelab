apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cah
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: cah-secret
    template:
      data:
        TOKEN: "{{ .token }}"
  dataFrom:
    - extract:
        key: cah
