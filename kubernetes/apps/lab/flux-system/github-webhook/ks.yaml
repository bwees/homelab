apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: github-webhook
spec:
  interval: 1h
  path: ./kubernetes/apps/lab/flux-system/github-webhook/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  dependsOn:
    # ExternalSecret pulls the HMAC token from 1Password
    - name: onepassword
      namespace: external-secrets
    # HTTPRoute needs the Gateway API CRDs + envoy-external gateway
    - name: envoy-gateway
      namespace: network
  targetNamespace: flux-system
