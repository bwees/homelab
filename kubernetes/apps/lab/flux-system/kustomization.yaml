apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: flux-system

# No namespace.yaml here: the flux-system namespace is owned by the bootstrap
# Kustomization, so re-declaring it would fight over ownership under prune.
resources:
  - github-webhook/ks.yaml
