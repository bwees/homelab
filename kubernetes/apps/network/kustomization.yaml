apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- namespace.yaml
# ks.yaml defines two Flux Kustomizations (envoy-gateway + envoy-gateway-config).
# The app/ and config/ manifests themselves are pulled in by those Flux
# Kustomizations (via spec.path), NOT listed here.
- envoy-gateway/ks.yaml
