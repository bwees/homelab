apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github-status-token
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: github-status-token
    template:
      data:
        token: '{{ .credential }}'
  dataFrom:
    - extract:
        key: flux-github-status
