{% if inventory_hostname in secrets %}
{% for key, value in secrets[inventory_hostname].items() %}
export {{ key }}="{{ value }}"
{% endfor %}
{% endif %}

{% for key, value in secrets.shared.items() %}
export {{ key }}="{{ value }}"
{% endfor %}